{
  "name": "tests-runtime",
  "private": true,
  "type": "module",
  "version": "0.0.0",
  "main": "lib/test-service.js",
  "scripts": {
    "test:runtime": "OUTPUTS_FILE=cdk.out/outputs.json jest --passWithNoTests && ./scripts/test-cli",
    "deploy:runtime": "OUTPUTS_FILE=cdk.out/outputs.json ./scripts/deploy",
    "test:runtime-deploy": "pnpm -r --filter tests-runtime run deploy:runtime && pnpm -r --filter tests-runtime run test:runtime"
  },
  "dependencies": {
    "@aws-sdk/client-sqs": "^3.241.0",
    "@eventual/aws-client": "workspace:^",
    "@eventual/cli": "workspace:^",
    "@eventual/core": "workspace:^"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.108",
    "@types/jest": "^29",
    "@types/node": "^16",
    "esbuild": "^0.x",
    "jest": "^29",
    "ts-jest": "^29",
    "ts-node": "^10.9.1",
    "typescript": "^4.9.4"
  },
  "jest": {
    "extensionsToTreatAsEsm": [
      ".ts"
    ],
    "moduleNameMapper": {
      "^(\\.{1,2}/.*)\\.js$": "$1"
    },
    "transform": {
      "^.+\\.tsx?$": [
        "ts-jest",
        {
          "tsconfig": "tsconfig.json",
          "useESM": true
        }
      ]
    },
    "testMatch": [
      "**/test/?(*.)+(spec|test).[jt]s?(x)"
    ]
  }
}
