#!/usr/bin/env bash

set -ex

export EVENTUAL_DEFAULT_SERVICE=eventual-tests

## start local as a daemon
npx pm2 --name Local start npx -- eventual local --entry 'test/test-service.ts'

## run the tests
pnpm run test:local

## stop the server
npx pm2 delete Local