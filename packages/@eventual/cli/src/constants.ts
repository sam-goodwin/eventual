export const NODE_VERSION = process.version;
export const PID = process.pid;
export const MACHINE_PLATFORM = process.platform;
export const MACHINE_ARCHITECTURE = process.arch;
export const EVENTUAL_VERSION = require("../package.json").version;
